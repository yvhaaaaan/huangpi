spring:
  datasource:
    url: jdbc:h2:mem:huangpi-test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: false

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-hmac-sha
    expiration: 1h
  storage:
    directory: ./target/test-uploads
    public-base-url: http://localhost
  wechat:
    mock-enabled: true
    app-id:
    app-secret:
